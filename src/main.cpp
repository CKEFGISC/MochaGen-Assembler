#include <bits/stdc++.h>
#include "../include/jngen.h"
#include "../include/json/json.h"
using namespace std;

struct object{
	string _id, _class;
	map<string, vector<string>> attr;
};

signed main(){
	vector<object> obj;
}
